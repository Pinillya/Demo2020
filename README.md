# DemoTools

Setup:

Clone the project
```
$ git clone https://github.com/Pinillya/DemoTools.git
```

run npm install
```
$ npm install
```

build the project
```
$ npm run build
```

start a webserver at port 8080
```
$ npm run start:dev
```